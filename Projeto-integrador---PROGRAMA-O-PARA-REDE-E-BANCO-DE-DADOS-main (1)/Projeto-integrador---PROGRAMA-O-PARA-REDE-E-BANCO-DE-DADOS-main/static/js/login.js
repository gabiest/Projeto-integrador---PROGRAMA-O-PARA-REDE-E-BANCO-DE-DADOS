// Minimal login behavior kept for demo
const loginForm = document.querySelector('form');
if (loginForm) {
  loginForm.addEventListener('submit', (e) => {
    // nothing special here for the demo; the form action navigates to /home.html
  });
}
